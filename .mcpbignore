# Not part of the MCP server bundle.

skill/
build-*.sh
*.skill

# Local / IDE config (not auto-excluded by the packer like .git is).
.claude/
.cursor/
.DS_Store

# Build artifacts (the packed bundle and any stray unpacked copy).
*.mcpb
Spark/
